(example
  (utterance "cars with mpg greater than 15")
  (targetValue (string "filter dim >= 15.0"))
)
(example
  (utterance "find cars with mpg greater than or equal to 15")
  (targetValue (string "find dim >= 15.0"))
)
(example
  (utterance "show cars with an mpg of 15")
  (targetValue (string "chart_type filter dim = 15"))
)
(example
  (utterance "show a histogram of cars with mpg equal to 15")
  (targetValue (string "chart_type filter dim = 15.0"))
)
(example
  (utterance "show cars with mpg no larger than 15 in a histogram")
  (targetValue (string "chart_type filter dim <= 15.0"))
)
(example
  (utterance "find name equals buick")
  (targetValue (string "find dim = buick"))
)
(example
  (utterance "draw only name being buick")
  (targetValue (string "filter dim = buick"))
)
(example
  (utterance "show only cars with mpg greater than 15")
  (targetValue (string "filter dim >= 15.0"))
)
(example
  (utterance "cars with mpg between 10 and 15")
  (targetValue (string "filter dim >= 10 <= 15"))
)
(example
  (utterance "search for cars with mpg between 10 and 15")
  (targetValue (string "find dim >= 10 <= 15"))
)
(example
  (utterance "show cars with name containing buick")
  (targetValue (string "chart_type filter dim contain buick"))
)
(example
  (utterance "show cars with a name that contains buick")
  (targetValue (string "chart_type filter dim contain buick"))
)
(example
  (utterance "show only cars with mpg greater than 13 and smaller than 15")
  (targetValue (string "filter dim >= 13.0 <= 15.0"))
)
(example
  (utterance "find cars with max mpg")
  (targetValue (string "filter dim max"))
)
(example
  (utterance "find 5 cars with minimum mpg")
  (targetValue (string "filter dim min 5.0"))
)
(example
  (utterance "show cars with amc name")
  (targetValue (string "filter dim contain amc"))
)
(example
  (utterance "find 5 percent of cars with maximum mpg")
  (targetValue (string "filter dim max 5 percent"))
)
(example
  (utterance "find 5 percent of cars with maximum mpg group by origin")
  (targetValue (string "filter dim max 5 percent dim"))
)
(example
  (utterance "sample 5 percent of cars")
  (targetValue (string "filter index random 5 percent"))
)
(example
  (utterance "sample 5 percent of cars group by origin")
  (targetValue (string "filter index random 5 percent dim"))
)
