(example
  (utterance "please highlight the selected data")
  (targetValue (string "highlight selection"))
)
(example
  (utterance "highlight selection from the plot")
  (targetValue (string "highlight selection from chart_type"))
)
(example
  (utterance "highlight selection in a histogram")
  (targetValue (string "highlight selection chart_type"))
)
(example
  (utterance "highlight selection from the plot in a parallel coordinates plot")
  (targetValue (string "highlight selection from chart_type chart_type"))
)
(example
  (utterance "show selection")
  (targetValue (string "chart_type selection"))
)
(example
  (utterance "show selected cars in a histogram")
  (targetValue (string "chart_type selection"))
)
(example
  (utterance "show mpg of the selected cars in a histogram")
  (targetValue (string "chart_type selection dim"))
)
(example
  (utterance "mpg of selection in histogram")
  (targetValue (string "chart_type selection dim"))
)
(example
  (utterance "selection in histogram")
  (targetValue (string "chart_type selection"))
)
(example
  (utterance "mpg of selection")
  (targetValue (string "chart_type selection dim"))
)
(example
  (utterance "show selected cars with mpg greater than 5")
  (targetValue (string "chart_type selection filter dim >= 5.0"))
)
(example
  (utterance "show mpg cylinders of selected cars with mpg greater than 5 in a scatterplot")
  (targetValue (string "chart_type selection dim dim filter dim >= 5.0"))
)
(example
  (utterance "select cars with mpg greater than 15")
  (targetValue (string "select dim >= 15.0"))
)
(example
  (utterance "select cars with mpg greater than 15 into a filter")
  (targetValue (string "filter selection dim >= 15.0"))
)
